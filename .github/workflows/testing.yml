name: C/C++ CI

on:
  push:
  pull_request:

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: Run cmake
      run: |
        cmake -B build
    - name: Run make
      run: |
        make -C build
        
